import Register from '@/components/pagesBoilerPlates/Register';

export const metadata = {
  title: "Register",
};

const RegisterPage = () => {
  return <Register></Register>
};

export default RegisterPage;