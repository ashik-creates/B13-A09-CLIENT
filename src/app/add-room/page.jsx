import AddRoom from '@/components/pagesBoilerPlates/AddRoom';

export const metadata = {
  title: "Add Room",
};

const AddRoomPage = () => {
  return <AddRoom></AddRoom>
};

export default AddRoomPage;